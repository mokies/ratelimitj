description 'RateLimitJ Hazelcast'

dependencies {

    compile(
            project(':ratelimitj-core'),
    )

    testCompile(
            project(':ratelimitj-internal:test'),
            libraries.assertj,
            libraries.junit,
            libraries.guava,
            libraries.mockito,
            libraries.logback,
    )

}
