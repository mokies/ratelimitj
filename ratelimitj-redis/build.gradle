description 'RateLimitJ Redis'

project.ext.release = true

dependencies {

    compile(
            project(':ratelimitj-core'),
            'biz.paluch.redis:lettuce:4.3.0.Final',
            'com.eclipsesource.minimal-json:minimal-json:0.9.4',
            'net.javacrumbs.future-converter:future-converter-rxjava-java8:0.3.0'
    )

    testCompile(
            project(':ratelimitj-internal:test'),
            libraries.assertj,
            libraries.junit,
            libraries.guava,
            libraries.mockito,
            libraries.logback,
    )
}
