description 'RateLimitJ Redis'

dependencies {

    compile(
            project(':ratelimitj-core'),
            libraries.assertj,
            libraries.junit,
            libraries.guava,
    )

}
